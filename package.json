{
  "name": "anthropic-ai-chat",
  "module": "main.ts",
  "type": "module",
  "scripts": {
    "start": "bun run main.ts",
    "build": "bun build --compile --target=bun main.ts --outfile=anthropic-ai-chat && rm -rf ./.*.bun-build"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.19.0",
    "@anthropic-ai/tokenizer": "^0.0.4",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.2.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
